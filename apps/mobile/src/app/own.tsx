/** Your own strategy — the design's stub, and the app's. */
import { StubScreen } from '@/ui/stub';

export default function OwnStrategyScreen() {
  return <StubScreen title="Your strategy" badge="COMING SOON" testID="own" />;
}
