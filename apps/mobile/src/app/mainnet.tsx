/**
 * Mainnet — where the network menu's second entry leads. Nothing here
 * switches anything: real money is a decision the app has not made yet.
 */
import { StubScreen } from '@/ui/stub';

export default function MainnetScreen() {
  return <StubScreen title="Mainnet" badge="COMING SOON" testID="mainnet" />;
}
