/** Invite friends — named in the design, not built yet. */
import { StubScreen } from '@/ui/stub';

export default function InviteScreen() {
  return <StubScreen title="Invite friends" testID="invite" />;
}
