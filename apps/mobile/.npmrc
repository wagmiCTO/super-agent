# @category-labs/mera 0.2.0 pins react-native-passkey 3.6.1 as a peer while we
# run 3.6.2. The two are compatible; mera is preview software and its pin is
# stale. Without this every install needs the flag by hand.
legacy-peer-deps=true
